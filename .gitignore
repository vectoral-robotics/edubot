# Imported package repos — never committed into the meta-repo.
/src/
/dev/

# Manifest staged by CI for the image build.
.build.repos

# Local env + colcon artifacts (if built on host).
.env
/build/
/install/
/log/

# OS / editor
.DS_Store
